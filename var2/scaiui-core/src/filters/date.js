import {format_date, format_time} from '../utils/date'

export const dateFormat = (value, params) => {
  return format_date(value, params)
}

export const timeFormat = (value, params) => {
  return format_time(value, params)
};




